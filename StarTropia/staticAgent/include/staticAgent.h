/*	Jesus Torres - 0640386	Alfredo Gallardo - 0630563*/#ifndef STATICAGENT_H#define STATICAGENT_H#include <iostream>#include <sstream>#include <GL/glut.h>// Clase StaticAgent usada para objetos que no se mueven en la escena.class staticAgent{	public:        //Atributos		GLfloat x,y,z;		bool Alive;		float minx,miny,minz;		float maxx,maxy,maxz;		float dminx,dminy,dminz;		float dmaxx,dmaxy,dmaxz;		bool collide;        //Funciones        staticAgent();		virtual ~staticAgent();				void Assign(GLfloat x,GLfloat y,GLfloat z);		void Update();		void Draw();				bool Collision(float minX, float minY, float minZ, float maxX, float maxY, float maxZ);		bool Collision2(float minX, float minY, float minZ, float maxX, float maxY, float maxZ);	private:		};#endif