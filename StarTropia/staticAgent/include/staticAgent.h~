#ifndef STATICAGENT_H#define STATICAGENT_H#include <iostream>#include <sstream>#include <GL/glut.h>class staticAgent{	public:        //Atributos		GLfloat x,y,z;		bool Alive;        //Funciones        staticAgent();		virtual ~staticAgent();				void Asign(GLfloat x,GLfloat y,GLfloat z);		void Update();		void Draw();				bool Collision();	private:		// posiblemente haya que agregar un bounding box o algo similiar para calular colisiones		};#endif